import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#023',
    maxWidth: '100%',
  },
  video: {
    flex: 1,
    maxHeight: '30%',
    backgroundColor: '#fff',
    borderColor: '#ff0',
    borderRadius: 10,
    marginBottom: 2,
  },
});

export default styles;
